<% title @self_user ? 'Mes Informations' : 'Informations de '+@user.fullname %>
<main>
	<div class="wrapper">

		<% if @self_user %>
			<h1>Mes informations</h1>
		<% else %>
			<h1>Informations de <%= @user.fullname %> </h1>
		<% end %>


		<%= render 'form', user: @user %>

	</div>
</main>
